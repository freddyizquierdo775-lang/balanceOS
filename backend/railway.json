{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "pip install httpx && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
